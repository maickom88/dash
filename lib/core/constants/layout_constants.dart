import 'package:flutter/material.dart';

class Layout {
  static const double defaultPadding = 13;
  static const double defaultBorderRadius = 13;
  static const double defaultGridSpacer = 15;
  static const double space30 = 30;
  static const double space20 = 20;
  static const double space10 = 10;

  static const List<BoxShadow> defaultBoxShadow = [
    BoxShadow(
      offset: Offset(0, 2),
      blurRadius: 10,
      spreadRadius: 0,
      color: Colors.black12,
    ),
  ];
}
